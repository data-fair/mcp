{
  "$id": "https://github.com/data-fair/simple-directory/api/config",
  "x-exports": [
    "types",
    "validate"
  ],
  "x-ajv": {
    "coerceTypes": "array"
  },
  "x-jstt": {
    "additionalProperties": false
  },
  "type": "object",
  "title": "Api config",
  "required": [
    "observer",
    "port",
    "transport"
  ],
  "properties": {
    "portalUrl": {
      "type": "string",
      "description": "Base URL of the Data Fair portal. Required in standalone mode (stdio transport).",
      "example": "https://opendata.koumoul.com"
    },
    "dataFairAPIKey": {
      "type": "string",
      "description": "API key sent to Data Fair for authentication. Used in standalone mode."
    },
    "ignoreRateLimiting": {
      "type": "string",
      "description": "Secret key sent to Data Fair to bypass rate limiting constraints. Used in stack mode."
    },
    "observer": {
      "type": "object",
      "properties": {
        "active": {
          "type": "boolean"
        },
        "port": {
          "type": "number"
        }
      }
    },
    "port": {
      "type": "number"
    },
    "transport": {
      "type": "string",
      "enum": ["stdio", "http"]
    }
  }
}